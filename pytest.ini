[pytest]
# Modules under test live at the repo root (parser.py, config.py, models.py),
# tests live in tests/. pythonpath = . puts the root on sys.path so the tests
# can `import parser` etc. without an editable install.
pythonpath = .
testpaths = tests
addopts = -q
